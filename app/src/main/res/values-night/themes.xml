<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EuPacienteApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.EuPaciente" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/ep_primary</item>
        <item name="colorOnPrimary">@color/ep_on_primary</item>
        <item name="colorSurface">@color/ep_bg</item>
        <item name="colorOnSurface">@color/ep_text</item>
        <item name="colorOnBackground">@color/ep_bg</item>

        <item name="android:statusBarColor">@color/ep_black</item>
        <item name="android:navigationBarColor">@color/ep_black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>
    </style>

</resources>