<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EuPacienteApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EuPacienteApplication" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@color/rc_blue_bg</item>
        <item name="android:statusBarColor">@color/rc_blue_bg</item>
        <item name="android:navigationBarColor">@color/rc_blue_bg</item>
    </style>

    <style name="Theme.EuPaciente.Login" parent="Theme.EuPacienteApplication" />

    <style name="Theme.EuPaciente.Splash" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/bg_splash_window</item>>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/azul_fundo</item>
    </style>

</resources>